{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Test Blog",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "cd ${workspaceRoot} && hexo s"
        }
    ]
}